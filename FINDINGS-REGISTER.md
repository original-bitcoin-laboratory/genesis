# Findings register

One row per finding: a stable ID that is not reused, the claim in one sentence, its evidence grade
(`docs/EVIDENCE_POLICY.md`; `EXECUTED (release build)` means witnessed on this laboratory's release
client and `EXECUTED (laboratory build)` on a binary it compiled from the archive's source, neither the unmodified
2009 binary; `RECORD` means read from a commit, a published document or a file header, not observed in execution), the artifact the claim rests on, and the date that artifact
was first committed. Cite the ID. IDs are for claims, not documents; a document that states several
claims appears on several rows, and a claim without an artifact is marked `pending:` until it has one.
`scripts/check_register.py` runs in CI and fails on a dangling or renamed ID anywhere in the three
repositories. The `OBL-C-` series (one row per consensus rule) is reserved for the constitution register.

Experimental laboratory research, in progress. Not money, not advice, no warranty (`RIGHTS.md`).

| ID | claim | grade | artifact | recorded |
|---|---|---|---|---|
| `OBL-F-0001` | v0.1 selects the best chain by block height, not cumulative proof-of-work: on a discriminating fork the challenger at height 125 with work 250 beats the incumbent at height 122 with work 262 (the laboratory's `jan09-faithful` profile run by `derivatives/netnode/test_chainstate.py`, whose result the artifact records: a model of the January rule, not the historical implementation) | `MODEL` | `paper-artifacts/height-vs-work.json` | 2026-08-01 |
| `OBL-F-0002` | v0.1 compares `nLockTime` as a block height only; the source has no `LOCKTIME_THRESHOLD`, so a wall-clock lock cannot be expressed | `JAN09-SOURCE + MODEL` | `derivatives/temporal/README.md` | 2026-07-27 |
| `OBL-F-0003` | Bitcoin's script resource limits were retrofitted in 0.3.6 on 29 Jul 2010 under a `makefile.unix wx-config` commit message; the 5000-byte element cap became 520 bytes on 15 Aug 2010 under "misc changes"; the January 2009 interpreter, compiled by this laboratory from the archive's `script.cpp` against its own compatibility shims, runs a 600-byte element and a 1500-deep stack | `RECORD + EXECUTED (laboratory build)` | `docs/SCRIPT-LIMITS-RETROFITTED.md` | 2026-08-12 |
| `OBL-F-0004` | v0.1's `CheckTransaction` accepts the output amounts of block 74638; the 0.3.10 rule (15 Aug 2010, commit `d4c6b90ca`) rejects them | `MODEL` | `derivatives/overflow/README.md` | 2026-07-27 |
| `OBL-F-0005` | the November 2008 pre-release's genesis coinbase carries the bare integer 247422313: no headline and nothing in the block that commits to a date (a 17 Nov 2008 mailing-list message dates an artifact of the archive's description, not these bytes) | `NOV08-SOURCE` | `docs/NOV08_GENESIS.md` | 2026-08-04 |
| `OBL-F-0006` | v0.1's retarget measures 2015 intervals against a 2016-interval budget; the fixed point is 600.30 s per block, 0.05% slow, not the 599.70 s a naive reading of the constants gives | `MODEL` | `derivatives/retarget/README.md` | 2026-08-01 |
| `OBL-F-0007` | v0.1 shipped a commerce subsystem that does not touch the transaction set: a flood pub/sub advert channel (`MSG_PRODUCT`, `MSG_TABLE`) and a local `CReviewDB`; the source shows the separation and gives no reason for it | `JAN09-SOURCE` | `inventory/MARKET_AUDIT.md` | 2026-07-26 |
| `OBL-F-0008` | for post-quantum signatures on a 2009-shaped chain, size and not verification is the binding constraint: transactions grow 19× to 86× | `MODEL` | `docs/PQ-SIGNATURE-COST.md` | 2026-08-11 |
| `OBL-F-0009` | two unmodified January 2009 binaries mine, relay, reorganise and spend a matured coinbase across the wire; the constant is `COINBASE_MATURITY` = 100, `ConnectInputs` refuses the spend while the coinbase sits fewer than 99 blocks below the tip (`main.cpp:824`), and the wallet withholds a generated coin until 120; the four process-image binding records (`bitcoin.exe` sha256 `fbcac071…`, the archive's binary; one process per node across the pre and post captures) are pinned by the set's sealed manifest and published beside it since 20 Sep 2026 | `JAN09-EXECUTED` | `r4-findings/2026-08-06-relayed-spend/FINDINGS.md` | 2026-08-09 |
| `OBL-F-0010` | the conformance corpus replayed over the wire against the release client (`c3f15fc5…`, OpenSSL 1.0.2u) agrees 136/136 scripts, 14/14 graded signature vectors and 19/19 blocks; the three DER probes, which carry no expected verdict, were rejected by that OpenSSL (an observation the corpus records, not an agreement) | `EXECUTED (release build) + MODEL` | `r5-findings/2026-09-13-binary-replay-release/FINDINGS.md` | 2026-09-13 |
| `OBL-F-0011` | v0.1 erases a block that fails `ConnectBlock` from disk and from the block index, so a rejected block is indistinguishable from one the node did not see; the best chain is chosen by height | `JAN09-SOURCE + EXECUTED (release build)` | `r5-findings/2026-09-13-binary-replay-release/FINDINGS.md` | 2026-09-13 |
| `OBL-F-0012` | the archives served under the name `bitcoin-0.1.0` contain v0.1.1: the byte size Satoshi gave in a 10 Jan 2009 email and a 10 Jan PE link stamp; no consensus rule differs | `RECORD` | `common/VERSION_LABEL.md` | 2026-08-08 |
| `OBL-F-0013` | `MAX_BLOCK_SIZE = 1000000` was defined in commit `a30b56ebe` (15 Jul 2010, 0.3.1, message about OpenSSL linkage and a tray icon) and used only by the miner; it became a block-validity rule in `f1e1fb4bd` (7 Sep 2010, message "cleanup, / catch some recoverable exceptions and continue"), gated on height > 79,400, so the first block it applies to is 79,401 (12 Sep 2010) | `RECORD` | `docs/MAX-BLOCK-SIZE-RETROFITTED.md` | 2026-09-19 |
| `OBL-F-0014` | for 30 Aug 2009 to 31 Dec 2010 the `bitcoin/bitcoin` history carries 132 of its 341 non-merge commits as pairs with the same first message line (121 with the `git-svn-id` trailer on one copy, 10 with it on both, 1 on neither) and one first line, "misc", eight times; in the one-trailer pairs the copy without the trailer is not earlier in any pair (median same day, at most 8.09 days later) and carries the author strings `s_nakamoto`, `Satoshi Nakamoto` or the literal `--author=Satoshi Nakamoto`; 124 of the 132 pairs are the same change, 8 differ by a few lines; the doubling starts at the root commit | `RECORD` | `docs/BITCOIN-GIT-HISTORY-PROVENANCE.md` | 2026-09-19 |
| `OBL-F-0015` | `MAX_BLOCK_SIGOPS = MAX_BLOCK_SIZE/50` entered in the same 7 Sep 2010 commit as the block-size rule, `f1e1fb4bd` | `RECORD` | `docs/MAX-BLOCK-SIZE-RETROFITTED.md` | 2026-09-19 |
| `OBL-F-0016` | the January 2009 release enforced a block-size ceiling: `CheckBlock` rejects a block whose serialised size or transaction count exceeds `MAX_SIZE` = 0x02000000 (32 MiB); the 2010 rule narrowed it, it did not create one | `JAN09-SOURCE` | `docs/MAX-BLOCK-SIZE-RETROFITTED.md` | 2026-09-19 |
| `OBL-F-0017` | cumulative-work chain selection replaced height in `3b7cd5d89` (25 Jul 2010, 0.3.3) under a message about JSON-RPC authentication and initial block download; the repository's first commit (30 Aug 2009, 0.1.5) selects by height | `RECORD` | `docs/CONSENSUS-ATLAS.md` | 2026-09-20 |
| `OBL-F-0018` | time-based `nLockTime` (the 500,000,000 height-or-time split) entered in `dd519206a` (29 Oct 2009) with a message naming "non-final tx locktime changes" and a source comment attributing it to 0.1.6 and deferring its use "until most 0.1.5 nodes have upgraded"; the first commit (0.1.5) compares heights only | `RECORD` | `docs/CONSENSUS-ATLAS.md` | 2026-09-20 |
| `OBL-F-0019` | the per-transaction size bound entered in two steps, 32 MiB in `401926283` (25 Aug 2010, "alert system / -- version 0.3.11") and 1 MB in `3df62878c` (13 Sep 2010, a message about the miner); `a790fa46f` (30 Sep 2010), cited earlier by this laboratory as the origin, moves the function unchanged | `RECORD` | `docs/CONSENSUS-ATLAS.md` | 2026-09-20 |
| `OBL-F-0020` | `IsStandard` entered as node policy in `a206a2398` (7 Dec 2010, gavinandresen) with a message stating the reason; a block carrying a non-standard transaction stays valid | `RECORD` | `docs/CONSENSUS-ATLAS.md` | 2026-09-20 |
| `OBL-F-0021` | the alert system entered in `401926283` (25 Aug 2010, "alert system / -- version 0.3.11") and left in four dated steps: removal merged 21 Mar 2016 (PR 7692, merge commit `29b2be6ad`, Core 0.13.0), final alert 19 Jan 2017, hard-coded in 0.14.0 (8 Mar 2017), key published 3 Jul 2018 | `RECORD` | `docs/CONSENSUS-ATLAS.md` | 2026-09-20 |
| `OBL-F-0022` | the Berkeley DB lock table was a block-validity rule with no commit: block 225,430 (11 Mar 2013) forked the chain, and the written response `8bd028818` (15 Mar 2013) limited a block to 4,500 distinct transaction ids until 15 May 2013 | `RECORD` | `docs/CONSENSUS-ATLAS.md` | 2026-09-20 |
| `OBL-F-0023` | OpenSSL's DER parser was a signature-validity rule with no commit until BIP 66 wrote strictness into consensus (`80ad135a5`, 13 Jan 2015; merged 3 Feb 2015 as `41e6e4cab`; enforced from 4 Jul 2015 under the 950-of-1,000 version-3 rule) | `RECORD` | `docs/CONSENSUS-ATLAS.md` | 2026-09-20 |
| `OBL-F-0024` | `CScriptNum` (PR 3965, 26 Mar to 9 May 2014, merge commit `1c0319bb2`) replaced OpenSSL's `BIGNUM` in script arithmetic without changing the rule; the four-byte operand cap is the 2010 rule of `4bd188c43` | `RECORD` | `docs/CONSENSUS-ATLAS.md` | 2026-09-20 |
| `OBL-F-0025` | the two rules nobody wrote, executed side by side: a block referencing 4,501 distinct transaction ids passes v0.1's `CheckBlock` size and count clauses and fails the 0.8.1 clause of `8bd028818` inside its 2013 window, and one referencing 4,500 passes both; BIP 66's `IsValidSignatureEncoding`, ported, passes the corpus's 7 strict signatures and fails its 3 probes, each of which a BER-tolerant reader recovers to an (r, s) that verifies under the vector's own key | `MODEL` | `derivatives/emergent/README.md` | 2026-09-20 |
| `OBL-F-0026` | at the measured post-quantum sizes a block at v0.1's own 32 MiB ceiling settles 281,000 to 1,270,000 one-input spends a day against 24.2 million under ECDSA, and a block at the 2010 1 MB rule 8,000 to 38,000 against 720,000; a hundred million outputs need 79 to 356 days of full blocks to each move once at 32 MiB and 7 to 33 years at 1 MB; the spend that migrates an exposed key is signed under the old scheme, so a second layer cannot make it | `MODEL` | `docs/PQ-SETTLEMENT-CAPACITY.md` | 2026-09-20 |
| `OBL-F-0027` | the January 2009 interpreter executes concatenation, multiplication, division, shifts and slicing natively (`OP_CAT`, `OP_MUL`, `OP_DIV`, `OP_LSHIFT`, `OP_SUBSTR` and ten more); the fifteen were disabled on 15 Aug 2010 under "misc changes" with no argument in the record, and a December 2023 construction (BitVM) verifies any computable function on Bitcoin without a rule change from hashlocks, timelocks and Taproot trees, at a cost its paper states as significant off-chain computation | `EXECUTED (release build) + RECORD` | `docs/VOCABULARY-ROUND-TRIP.md` | 2026-09-20 |
| `OBL-F-0028` | the January 2009 client replaces a held transaction with a newer version of itself (same inputs, higher `nSequence`) and erases the old one; commit `05454818d` (19 Aug 2010, a message about block-index checking) disables it with two lines, and the replacement code stays unreachable below them | `JAN09-SOURCE + MODEL` | `derivatives/origin_policy/README.md` | 2026-09-20 |
| `OBL-F-0029` | the January 2009 client names no block but the genesis; hard-coded checkpoints entered on 17 Jul 2010 (`ae922a36a`, three heights, "security safeguards") and reached five by 15 Aug 2010, the last at 74000 committed hours before the overflow block 74638 | `JAN09-SOURCE + MODEL` | `derivatives/origin_policy/README.md` | 2026-09-20 |
| `OBL-F-0030` | the January 2009 client's only fee rule, one cent per started kilobyte with a 10 KB discount, is applied when a node builds a block or sends a transaction and not when it receives one; from 12 Dec 2010 (`97ee01ad8`, "added some DoS limits") relay refuses a transaction below the rule and rate-limits free ones to 150,000 bytes per ten minutes | `JAN09-SOURCE + MODEL` | `derivatives/origin_policy/README.md` | 2026-09-20 |
| `OBL-F-0031` | the 15 August 2010 overflow incident dated from three records: the fix posted as code by 21:00 UTC, committed at 21:35 (r132) and released before midnight; the corrected chain's block 74638 timestamped 23:53:59, 6 h 51 min after 74637; the checkpoint at 74000 committed at 21:05 the same evening under "misc changes"; no block removed by decree, a competing chain winning under new rules | `RECORD` | `docs/INCIDENT-2010-08-15.md` | 2026-09-20 |
| `OBL-F-0032` | in the January 2009 interpreter `OP_VERIFY` on false and `OP_RETURN` end execution by setting `pc = pend` rather than failing it (`script.cpp:156-173`), so the script completes with its stack as it stands; six corpus vectors depend on it; bytes 0xb0-0xef are undefined and fail the script (`default: return false`, `script.cpp:797`), and a first byte at or above 0xf0 begins a two-byte opcode | `JAN09-SOURCE + MODEL` | `common/conformance/CONSENSUS_BEHAVIORS.md` | 2026-09-20 |
| `OBL-F-0033` | the executed coinbase-maturity rule is a fencepost below its constant: `ConnectInputs` refuses a coinbase spend while `nBestHeight - height < COINBASE_MATURITY - 1`, so the spend is allowed once the coinbase is 99 blocks below the tip, not 100 (`main.cpp:824`); the wallet's `GetBlocksToMaturity` uses `COINBASE_MATURITY + 20` | `JAN09-SOURCE` | `common/conformance/CONSENSUS_BEHAVIORS.md` | 2026-09-20 |
| `OBL-F-0034` | v0.1's unbounded `CBigNum` accepts a 9-byte numeric operand that the 2010 four-byte cap (`nMaxNumSize = 4`, `4bd188c43`) rejects by throwing in `CastToBigNum`; the cap reads operands, not results, so `1 << 64` still passes under it | `MODEL` | `derivatives/script_limits/test_numeric_cap.py` | 2026-09-20 |
| `OBL-F-0035` | the op-count limit entered in `6ff5f718b` (31 Jul 2010, 0.3.7, "additional security limits") as `nOpCount++ > 200`, with the script-size cap lowered from 20,000 to 10,000 in the same commit, and took the form `opcode > OP_16 && ++nOpCount > 201` in `f1e1fb4bd` (7 Sep 2010); on the ported rule 201 counted opcodes pass and 202 fail, and neither the 29 Jul nor the 15 Aug 2010 commit carries the limit | `RECORD + MODEL` | `derivatives/script_limits/README.md` | 2026-09-20 |
| `OBL-F-0036` | `IsStandard` is a relay-and-mining policy, executed: the January 2009 memory-pool path relays a hash-lock transaction that the 7 Dec 2010 clause of `a206a2398` refuses as nonstandard (two output templates, `GetSigOpCount() > 2`, under 100 bytes), while `CheckTransaction` accepts it in both eras | `MODEL` | `derivatives/origin_policy/is_standard.py` | 2026-09-20 |
| `OBL-F-0037` | the 0.3.12 release announcement of 7 Sep 2010 (mirror post 440) lists four changes and not the 1 MB block-validity rule its code carries; the author's posts of 3 and 4 Oct 2010 (mirror posts 478, 485) refuse a patch raising the limit and state a phase-in method, with no argument for the size; the 0.3.11 announcement (post 429) states the alert system's reasons and the 0.3.19 announcement (post 543) the relay gate's | `RECORD` | `CONSTITUTION-REGISTER.md` | 2026-09-20 |

## Series

- `OBL-F-nnnn`: findings (claims about the sources, the binaries or the reconstructions).
- `OBL-C-nnnn`: consensus-rule entries of the constitution register, one per rule, each with the commit that
  introduced it, its message verbatim, whether the message describes the change (`message_match`), the argument
  the record carries for it, and an executed witness or a stated reason for its absence. Not yet started; the
  checker reports its `message_match` tally with the number of surface rows as the denominator, counting rules,
  with origin commits as a sub-count (a rule with several commits lists them as `sha:false,sha:true`).
