[package]
name = "miner"
version = "0.1.0"
edition = "2021"
description = "Dependency-free multi-threaded SHA-256d nonce search for 80-byte Bitcoin headers (NOT money)"
license = "MIT"

[dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
