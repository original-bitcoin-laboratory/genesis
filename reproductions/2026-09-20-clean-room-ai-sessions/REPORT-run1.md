# Clean-room run 1 — report as received, 20 September 2026

*Recorded verbatim from the session's output, with its own label as its first line. The session was
an AI session commissioned by the operator, working from the brief in `../../REPRODUCTIONS.md`, with
no access to this laboratory's workspace. Its runtime could not reach GitHub raw content or
`bitcoin/bitcoin`; the report says so. Its validator is `run1-cleanroom_jan09b_validator.py` beside
this file. Nothing here has been edited by the laboratory; corrections it prompted are in the
registers, dated.*

---

Clean-room reproduction by a party with no access to the laboratory's workspace; independent of its code; not independent of its published documents.

| item | what you did | result | what was missing from the public material |
| --- | --- | --- | --- |
| A — JAN09-B specification | Read `genesis/docs/JAN09-B-SPECIFICATION.md` and the constitution-register rows it cites. I did **not** read `derivatives/validator-rs` or `derivatives/model`. | A clean-room Python validator for the page's explicit numeric/rule surface was written and sanity-tested. `MAX_MONEY = 2,100,000,000,000,000`; `MAX_SIZE = 33,554,432`; `MAX_BLOCK_SIGOPS = 671,088`; script caps 520 / 10,000 / 1,000 / 4; nLockTime threshold `500,000,000`. | The page is not a complete executable consensus specification. |
| A — MoneyRange / output sum | Implemented per-output `[0, MAX_MONEY]` and running output-sum range checks. | Fully reproducible from the page. | None for those stated bounds; surrounding transaction-validity rules are inherited rather than fully restated. |
| A — script caps | Implemented the four stated ceilings as supplied measurements: element 520 bytes, script 10,000 bytes, stack depth 1,000, numeric operand 4 bytes. | Numeric limits are reproducible. | The page does not define the exact operational accounting: e.g. which stacks count toward the depth, precisely where numeric operands are measured, or the complete interpreter semantics needed to obtain those measurements. |
| A — 32 MiB block ceiling | Implemented `serialized_size <= 0x02000000`. | The 32 MiB boundary itself is reproducible. | The page also says "transaction count" but does not define exactly how that count is constrained or whether the two checks are meant independently/identically. |
| A — sigop rule | Implemented the stated `MAX_SIZE / 50` ceiling and tested boundary behavior. | The numerical ceiling is reproducible. | The page does not give the sigop-counting algorithm: which opcodes count, how multisig is counted, or other interpreter details. Integer-division/rounding semantics are also unstated; my implementation used floor division as an explicit implementation assumption. |
| A — cumulative-work chain selection | Implemented selection from **supplied cumulative-work values** and rejected ties as `UNSPECIFIED`. | The stated comparison rule is reproducible. | The page says "cumulative work as in `3b7cd5d89`" but does not itself specify the per-header work calculation or a tie-break rule. The register likewise records the principle rather than a self-contained algorithm. |
| A — time-based nLockTime | Implemented the `500,000,000` threshold distinction. | Threshold rule reproducible. | Finality interaction is incomplete: the page does not fully state sequence-number semantics, comparison edge cases, or unambiguously identify the time source to use for this check. The corpus README separately says its block replay supplies wall-clock time and does not consult finality, which is not enough to close the specification gap. |
| A — strict DER | The validator deliberately reports this rule as **UNSPECIFIED** rather than importing an external DER grammar. | This is the decisive clean-room gap. | JAN09-B says to use `IsValidSignatureEncoding` "as in BIP66", but does not reproduce the parser/grammar. BIP66 was outside the named laboratory material and I did not use it. Therefore a stranger cannot independently implement the exact strict-DER predicate from the page alone. |
| A — overall constructibility | Treated the page as a specification rather than filling omissions from historical Bitcoin Core. | **A stranger can build the stated bounds/rules surface, but not a complete consensus validator from the page alone.** | In addition to DER, the page delegates major consensus behavior to inherited January-2009 implementation semantics without giving a self-contained specification. It explicitly lists several further open questions, including DER acceptance on the 2009 binary and numeric-cap execution cases. |
| B — corpus acquisition | Located the seven published suites and the corpus README. The README states 317 vectors: 136 evalscript, 15 retarget, 9 merkle, 4 headers, 15 sighash, 17 checksig, 127 blocks. | Corpus structure and stated expectations were independently read. | The runtime could not fetch/materialize the JSON corpus files for execution: GitHub/raw access from the execution environment failed at the network layer. I therefore did **not** claim a replay I could not run. |
| B — `evalscript.json` (136) | Identified as v0.1 `EvalScript`. | **Not replayed.** | The suite tests broad Script behavior, much of which is outside the explicit Task-A validator surface; the page itself does not restate complete Script semantics. |
| B — `retarget.json` (15) | Identified as `GetNextWorkRequired`. | **Not replayed.** | This is not one of the concrete Task-A checks requested; a full clean-room replay would require the applicable retarget algorithm and boundary cases. |
| B — `merkle.json` (9) | Identified as odd-duplication merkle construction. | **Not replayed.** | Again outside the requested executable surface; the page only states the inherited rule rather than supplying a full standalone implementation specification. |
| B — `headers.json` (4) | Identified as 80-byte headers, double-SHA256 and PoW. | **Not replayed.** | Full header/PoW semantics were not needed for the validator written in A. |
| B — `sighash.json` (15) | Identified as `SignatureHash`. | **Not replayed.** | Complete SIGHASH serialization behavior is not part of the Task-A validator I constructed. |
| B — `checksig.json` (17) | Identified as signature verification/checksig/CHECKMULTISIG; README says three non-strict-DER probes have `expected_binary = null`. | **Not replayed.** No agreement/disagreement is asserted. | The corpus README itself says those three cases were not replayed against the unmodified January-2009 binary, whose OpenSSL 0.9.8 behavior is treated separately. |
| B — `blocks.json` (127) | Identified as block/orphan/accept/index/connect/reorg cases. | **Not replayed.** No per-vector disagreement classification can responsibly be made. | README says the corpus uses easy-work blocks, that the real 2009 binary rejects them for minimum-work reasons, and that its block coverage does not include retarget boundaries, equal-height reorgs, or full Script execution inside blocks. |
| B — agreements/disagreements | I did not substitute the laboratory's published replay counts for my own execution. | **No independently measured agreements or disagreements were obtained.** Consequently there are no responsibly reportable disagreement vectors to classify as "my bug", "page ambiguity", or "corpus wrong". | The missing executable access to the published JSON corpus is an environment limitation; additionally, the corpus README itself documents un-replayed 2009-binary cases. |
| C — constitution row: `OBL-C-0003` | Selected the MoneyRange/output-sum row, origin `d4c6b90ca`. The register records date `2010-08-15` and message `fix for block 74638 overflow output transaction`. | **Register entry read; origin-commit confirmation not independently completed.** | Direct retrieval of the `github.com/bitcoin/bitcoin` commit page failed in this runtime, so I could not independently confirm the commit's author string and exact diff-added line from GitHub. |
| C — constitution row: `OBL-C-0007` | Selected the nLockTime row, origin `dd519206a`. The register records `2009-10-29` and the verbatim message `addr relaying fixes, proxy option and privacy patches, detect connect to self, non-final tx locktime changes, fix hide unconfirmed generated`. | **Register entry read; origin-commit confirmation not independently completed.** | Same GitHub retrieval limitation; therefore I will not pretend to have independently checked date, author string, message, and diff line. |
| C — constitution row: `OBL-C-0008` | Selected cumulative-work chain selection, origin `3b7cd5d89`. The register records `2010-07-25` and the message `Gavin Andresen's JSON-RPC HTTP authentication, faster initial block download -- version 0.3.3`. | **Register entry read; origin-commit confirmation not independently completed.** | Same limitation. I could not fetch the origin commit from GitHub sufficiently to certify the author string and exact added line. |
| C — overall record test | Compared what the register itself asserts with what I could independently retrieve. | **The record did not get an independent pass/fail determination.** | The missing direct origin-commit retrieval prevents claiming that the register "held" on all four requested fields. |

**Environment:** Linux `6.18.44-x86_64`, glibc 2.41; Python 3.13.5 (GCC 14.2.0); OpenSSL 3.5.5. Validator was written from scratch in Python. No laboratory validator/model source was read.

**Commit IDs read from the public constitution register:** `a30b56ebe`, `f1e1fb4bd`, `172f00602`, `757f0769d`, `4bd188c43`, `d4c6b90ca`, `76793dc96`, `dd519206a`, `cc0b4c3b`, `3b7cd5d89`, `40cd03694`, `401926283`, `3df62878c`. The three selected for the requested origin check were `d4c6b90ca`, `dd519206a`, and `3b7cd5d89`.

**Three one-line answers**

Could a stranger build from the page? **Not a complete consensus validator: the page leaves strict DER grammar, sigop accounting, several Script-accounting details, cumulative-work calculation/tie-breaking, and parts of nLockTime finality unspecified or delegated.**

Does the corpus replay outside the laboratory's code? **This reproduction did not establish that by execution: the README describes a largely self-contained replay apparatus, but I could not independently execute the 317 JSON vectors in this environment.**

Did the record hold? **Not independently established: the three register entries were read, but the corresponding `bitcoin/bitcoin` origin commits could not be fetched and checked here, so no independent pass/fail claim is justified.**
