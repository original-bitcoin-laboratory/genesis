# Adversarial review 2 — report as received, 20 September 2026

*Recorded verbatim from the session's output, in two passes as the session produced them (the
operator typed "Continue" between them). The session was an AI session commissioned by the
operator, working from the adversarial-review brief in `../../REPRODUCTIONS.md`, with no access to
this laboratory's workspace. It cloned the three public repositories and a blobless
`bitcoin/bitcoin`, ran the derivative suites, and could not reach the hosts `scripts/fetch-artifacts.sh`
pulls from. Nothing here has been edited by the laboratory; what was corrected from it, what was
disputed and why, is in `../../REPRODUCTIONS.md`, dated.*

---

Adversarial review by a party with no access to the laboratory's workspace; internal to no one; findings are the reviewer's own.

Method and its limits. I cloned original-bitcoin-laboratory/{genesis,common,pre-genesis} and a blobless bitcoin/bitcoin, verified commit metadata and diffs with git, recomputed the 2009–2010 commit census, and ran the five named derivative suites (all pass: 10/10/9/12/11). I did not reach scope items 4 (PQ recomputation), 5 (language rule) or the full sweep of item 1 before running out of tool budget. The table below is therefore partial, and I flag that as a limitation of this review, not as a clean bill for the parts unexamined. I also never obtained the January 2009 source (scripts/fetch-artifacts.sh pulls from hosts outside my egress allowlist), so every JAN09-SOURCE grade in the register is unverified by me — a significant gap, since that grade is load-bearing.

| file · line | claim | defect | severity | fix |
| --- | --- | --- | --- | --- |
| docs/CONSENSUS-ATLAS.md ~164–168 | "Introduced in 401926283 … Message, verbatim: alert system" | The message is alert system -- version 0.3.11. A document whose stated method is verbatim quotation truncates one. Repeated at FINDINGS-REGISTER.md OBL-F-0019, OBL-F-0021 and CONSTITUTION-REGISTER.md OBL-C-0009/0010. | wrong | Quote the full first line, or mark the elision. |
| docs/CONSENSUS-ATLAS.md 64, 177; CONSTITUTION-REGISTER.md OBL-C-0009 | "PR 7692 (9e17aac6b, 2016-03-21)" | No such object in bitcoin/bitcoin. The PR 7692 merge is 29b2be6ad. | wrong | Replace the sha. |
| docs/CONSENSUS-ATLAS.md 334 | "merged 2015-02-03 (bd03a1cb9)" | No such object. PR 5713's merge is 41e6e4cab. | wrong | Replace the sha. |
| docs/CONSENSUS-ATLAS.md 263 | "pull request 3965 … merged 2014-05-09 (681f02551)" | No such object. PR 3965's merge is 1c0319bb2. | wrong | Replace the sha. |
| docs/CONSENSUS-ATLAS.md 177 | "commits by BtcDrak 6–18 March 2016" | The 18 March commit in that PR (6601ce508) is Thomas Kerin's. | wrong | Name the authors, or give the range without attributing all of it. |
| docs/BITCOIN-GIT-HISTORY-PROVENANCE.md 5–6, 46; OBL-F-0014 | "one copy with the git-svn-id trailer and one without" / "122 groups of one trailer copy + one non-trailer copy" | Ten pairs have the trailer on both copies — a contiguous run r148–r157 (f1e1fb4bd/8c9479c6b, 3df62878c/71cc095cb, 172f00602/6aeb45187, and six more), plus one pair with neither. Three of these are origin commits the register relies on. My recount of the note's own method gives 123 one-trailer pairs, not 122. | wrong | State the three pair types and their counts; re-derive the total. |
| docs/BITCOIN-GIT-HISTORY-PROVENANCE.md 49 | "non-trailer author s_nakamoto 76 · Satoshi Nakamoto 25 · --author= 7 · sirius-m 12 · Gavin Andresen 5" | Sums to 125, against the 122 pairs asserted three lines above. My recount: 72/25/8/13/5 = 123. | wrong | Recompute; publish the pair list. |
| docs/BITCOIN-GIT-HISTORY-PROVENANCE.md 80–81 | "The trailer copy carries … the earlier date in every pair. This laboratory cites that copy" | The stated citation rule is undefined for the ten both-trailer pairs, which share revision, date and author. For f1e1fb4bd the two copies' messages differ (…continue vs …continue -- version 0.3.12 release), so the message_match verdict in OBL-C-0001/0004 depends on an unstated choice. | unsupported as written | State the tie-break rule. |
| CONSTITUTION-REGISTER.md OBL-C-0001, OBL-C-0004 | lineage_note | Does not record 172f00602's duplicate 6aeb45187, though the register's own field definition requires it. | unsupported as written | Add it. |
| docs/CONSENSUS-ATLAS.md 104–107 | the dd519206a diff, quoted as two added lines | Non-contiguous lines presented as contiguous, with no ellipsis. The dropped line — "do not use time based until most 0.1.5 nodes have upgraded" — is the only line in the commit that gives a reason, and the row grades the argument not-in-record. | wrong | Quote the hunk, and re-examine the argument cell. |
| docs/EVIDENCE_POLICY.md 52 | "scripts/revise_findings.py is the instrument, and its edit table is the record" | No such file exists in any of the three repositories. The whole revision discipline rests on an instrument not published. | wrong | Publish it or drop the sentence. |
| derivatives/overflow/overflow.py 8 | "The fix (0.3.1, Aug 2010) added MoneyRange()" | 0.3.1 shipped 16 July 2010. d4c6b90ca carries VERSION = 310 — 0.3.10, as OBL-F-0004 and the directory's own README correctly say. | wrong | Correct to 0.3.10. |
| derivatives/emergent/test_emergent.py 46–50 | test_081_boundary_is_exactly_4500 | block_with_unique_ids yields 1+2k, always odd; 4500 is unconstructable. The "boundary" is carried by assert MAX_UNIQUE_TXIDS == 4500 — a module constant against a literal in the same authorship. | unsupported as written | Build a 4500-id block (even one duplicate id suffices) and assert it passes. |
| derivatives/emergent/test_emergent.py 27–31 | test_v01_accepts_a_block_with_4501_unique_txids | The ported v0.1 side checks only non-empty, count ≤ 32 MiB, size ≤ 32 MiB. The block is 139,647 B. The assertion reduces to 140 KB < 32 MiB and would pass on an engine that accepted everything of that shape. | unsupported as written | Either port the rest of CheckBlock, or rename the test to what it checks. |
| derivatives/emergent/der_strictness.py 16–17 | "each of the three probes … is read by the tolerant reader to the same (r, s) as its strict twin" | No twin exists: each probe's (r, s) is unique in the corpus. Under the other reading — the strict re-encoding of itself — the claim is circular. The atlas's weaker §9 wording is the accurate one. | wrong | Add strict twins sharing (r, s), or adopt the atlas wording. |
| derivatives/emergent/test_emergent.py 90–99 | test_the_probes_differ_from_strict_only_in_encoding | is_valid_signature_encoding(encode_strict(r,s,flag)) is true by construction of encode_strict for any (r, s). It tests the encoder, not the probes. | unsupported as written | Assert the recovered (r, s) equals an independently-known pair. |
| derivatives/overflow/test_overflow.py 32 | assert not ok and "MoneyRange" in why | Substring match against a string the module itself emits. | style | Assert the rejecting clause, not its label. |
| derivatives/overflow/test_overflow.py 44–50 | test_the_divergence_is_the_whole_bug | tiny_input >= -997538 is arithmetic written in the test. v0.1's ConnectInputs is not ported, so the inflationary consequence — the actual claim — is never executed. | unsupported as written | Port the value-in/value-out check. |
| derivatives/retarget/retarget.py 58–67; test_retarget.py 21–23 | intervals_measured() "Run[s] the pindexFirst walk exactly as main.cpp:700-702" | It increments a counter N_INTERVAL-1 times and returns it. No chain, no pprev. The test then asserts N_INTERVAL-1 == N_INTERVAL-1. | unsupported as written | Walk an actual index chain, or state that the value is read from source. |
| derivatives/retarget/test_retarget.py 87–91 | test_constants_match_main_cpp | Compares each constant to its own defining expression in the same file. No main.cpp is read. | unsupported as written | Check against a hash of the fetched source. |
| derivatives/retarget/retarget.py 38–51; README §C | set_compact returning (value, negative, overflow), presented as v0.1 fidelity | That triple, and the size > 34 bound, are Bitcoin Core's later arith_uint256::SetCompact. v0.1 used CBigNum::SetCompact over OpenSSL MPI with no such returns. The module claims a line-for-line port of main.cpp:685-728; this codec is not in that range. | wrong | Attribute the codec to its real origin. |
| derivatives/retarget/README.md 17, 44–45 | "after 5 periods the attacker's target is ~1000x easier" | base = POW_LIMIT // 1000 is chosen by the author; the attack clamps at the pow limit, so "1000×" is the chosen starting difficulty restated. The mechanism gives 4×/period. | style | Report 4^n and the clamp; drop the derived figure. |
| derivatives/retarget/README.md 34 | "the opposite direction from the common '~599.7 s, a hair fast' phrasing" | No source is named for the phrasing being corrected. | self-asserted | Cite it or drop it. |
| derivatives/script_limits/README.md 7, 43 | the op-count limit "201", tested as an exact boundary | 757f0769d installs nMaxNumSize = 258, script size 20000, element 5000, stack+altstack 1000 — no op counter. 4bd188c43 adds the disabled opcodes and tightens 5000→520, 258→4. Neither adds a 201-op limit, and CONSTITUTION-REGISTER.md OBL-C-0002 correctly omits it. | wrong | Date the op limit to the commit that actually introduced it, or drop it. |
| CONSTITUTION-REGISTER.md OBL-C-0012; derivatives/script_limits/ | "derivatives/script_limits/ has no numeric-operand case" | The suite tests a cap the register does not attribute to any commit and omits the one it does (258→4). The gap is disclosed, which is to its credit, but the directory is still mis-scoped against its own register. | unsupported as written | Add the numeric case. |
| derivatives/emergent/README.md, docs/CONSENSUS-ATLAS.md §9 | the DER rule as "emergent" | The acceptance side — that OpenSSL 0.9.8 accepted these encodings — has no evidence at any grade. The corpus records expected_binary: null for all three probes. The rejection side is a port of BIP 66 against vectors the laboratory generated. | unsupported as written | Keep the row open, and say so in the README as plainly as the register does. |

Verified and not defective, for the record: every 2009–2010 commit sha, author string, SVN revision and author date I checked (dd519206a r18, a30b56ebe, 3b7cd5d89 r109 with 18 files/+405/−137, 757f0769d, d4c6b90ca, 4bd188c43, 401926283 r142, f1e1fb4bd r148, 3df62878c r154, 172f00602 r156, a206a2398 r197 with 6 files/+49/−11, aa496b75c, 8bd028818, fc6deb521, 80ad135a5 +63/−63, 48d8eb184, 05e3ecffa) is correct. The 172f00602 correction of 20 September is right: it does replace MAX_SIZE with MAX_BLOCK_SIZE in CheckBlock and strip the nHeight > 79400 gate. The a790fa46f self-correction is right: that commit moves CheckTransaction without changing it. The quoted diffs in §§2, 3, 5, 6, 8 match byte for byte. And the census reproduces exactly: 385 commits, 42 merges, 202 trailers, last trailer 2010-12-17T20:16:27Z, the eight author-string counts, the 34 satoshin@gmx.com addresses, all eight differing pairs with their exact +/− figures, and the offset distribution (min 0.00, median 0.00, p90 1.75, max 8.09, none earlier). The two pairs recorded as "not compared" are in fact identical. This is unusually careful work, which is why the failures above matter: they are failures of a method that mostly holds.

What the record does not address. The central claim is that most of Bitcoin's consensus rules were retrofitted under commit messages that do not describe them, and that the January 2009 design is therefore a distinct constitutional object. The record never confronts the obvious rejoinder: SVN commit messages in 2010 were release summaries, not per-change descriptions, and the message_match metric is therefore measuring a property of the version-control practice rather than a property of the rules. 757f0769d's message is "reverted makefile.unix wx-config -- version 0.3.6" — a release marker. Under the laboratory's own metric, a project that batches a fortnight of work into one commit per release scores false almost everywhere, and a project that commits atomically scores true almost everywhere, with no difference in how deliberate any rule was. Six false verdicts out of thirteen is exactly what one would expect from release-batched commits and tells you nothing further. The register's "Cite; do not characterise" is the right instinct, but message_match is itself a characterisation — of silence — and nothing in the record tests it against the null hypothesis. The adjacent evidence that would settle it (the bitcoin-list and forum threads of July–September 2010, where Satoshi discusses the size and script limits in the open, and Gavin's own posts on the block-74638 response) is never cited, in a record that cites BIPs, alert pages and release notes freely.

The single weakest point. It is not any of the errors above; it is the JAN09-SOURCE grade itself. Twenty-six findings rest on a January 2009 source tree that no one outside the laboratory can obtain from the repository: scripts/fetch-artifacts.sh reaches for an external archive, manifests/CHAIN_SOURCE_MANIFEST.json records hashes of files that are not present, and every line citation in the derivatives (main.h:442, main.cpp:685-728, main.h:1086) points at a file the reader does not have and cannot check. The laboratory has built an elaborate apparatus — timestamped seals, signed notes, an evidence grammar, a dangling-ID checker — around a base artifact whose absence makes the whole JAN09-SOURCE column self-asserted in exactly the sense the apparatus was built to prevent. OBL-F-0012 compounds this: it concedes that "the archives everyone serves as bitcoin-0.1.0 contain v0.1.1", which means the base is not even the object its own filename claims, and the resolution of that discrepancy rests on an email and a PE timestamp rather than on anything a reader can re-derive. The laboratory's most-repeated methodological line is that a claim about a 2010 commit should name the hash it read, not the message. The same standard, applied to its own foundation, is not met: it names a version label and a manifest, not a tree anyone else can reach.

---

*(second pass, after the operator typed "Continue")*

Adversarial review by a party with no access to the laboratory's workspace; internal to no one; findings are the reviewer's own.

Second pass. Completing the scope: the grade audit on FINDINGS-REGISTER.md (item 1), recomputation of the PQ figures (item 4), and the language rule (item 5). All 27 register artifacts resolve; both PQ scripts run; I recomputed every figure in both PQ documents, not three.

| file · line | claim | defect | severity | fix |
| --- | --- | --- | --- | --- |
| r4-findings/2026-08-06-relayed-spend/FINDINGS.md 75–76, 83; FINDINGS-REGISTER.md OBL-F-0009 | "two unmodified January 2009 binaries … the artifacts are bound to the executing image, not just to a file in an archive" — graded JAN09-EXECUTED | The directory contains no EXECUTED_BINARY_BINDING_*.json, and EVIDENCE_MANIFEST.json never mentions bitcoin.exe. The only identification of the client is the prose line "from the January 2009 archive, C:\obl\bitcoin.exe, unmodified" — no digest, no process record. Meanwhile all 24 published binding records in bitcoin-findings/ bind C:\bitcoin\bitcoin-0.1.3\bitcoin.exe, sha256 c3f15fc5…, which derivatives/vectors/README.md:4 identifies as "this lab's release bitcoin.exe". The binding apparatus exists (derivatives/r4/tools/capture_binding.ps1) and was applied to every run that used the laboratory's own build, and to none of the runs the register grades JAN09-EXECUTED. | unsupported as written | Publish the binding pair and the archive binary's digest, or regrade to the prose's actual warrant. |
| docs/SCRIPT-LIMITS-RETROFITTED.md 149–153; OBL-F-0003 | "the January 2009 interpreter runs a 600-byte element and a 1500-deep stack" at JAN09-EXECUTED | The note says the interpreter "was compiled from the authentic script.cpp" — by the laboratory, against its own shims in derivatives/build-reconstruction/compat/. Under EVIDENCE_POLICY.md:14 that is EXECUTED (release build), not "reproduced with the historical implementation". The laboratory draws this distinction correctly in OBL-F-0010 and OBL-F-0011 and not here. | wrong | Regrade to EXECUTED (release build). |
| docs/SCRIPT-LIMITS-RETROFITTED.md 168–171 | "script.cpp was unchanged for seven months, from the January 2009 release to the first commit … for this file, provenance no longer rests on custody and published digests alone" | The comparison is between bitcoin/bitcoin@e071a3f6 and the SNI archive. Two byte-identical copies show they are the same object; they do not date either to January 2009 — both could descend from the same later object, and the archive's date rests on custody. The inference runs backwards. OBL-F-0012 additionally holds that that archive is v0.1.1, not v0.1.0, so "the January 2009 release" is the wrong referent. "Two things follow" — one does. | unsupported as written | State it as: the file is identical in two independent custodies. |
| FINDINGS-REGISTER.md OBL-F-0012 | "the byte size Satoshi gave in a 10 Jan 2009 email and a 10 Jan PE link stamp", graded JAN09-SOURCE | JAN09-SOURCE is defined as "visible in the January source release". An email and a PE header are neither in the source nor in the release's text. Also "the archives everyone serves" is an absolute over an unenumerated set. | wrong | New grade for external documentary evidence; drop "everyone". |
| FINDINGS-REGISTER.md OBL-F-0017 … OBL-F-0024, OBL-F-0021 in particular | eight rows graded DESCENDANT | DESCENDANT is defined as "observed in a later Bitcoin implementation". These rows record commit messages, merge dates, BIP assignment dates and bitcoin.org alert-page dates — none is an observation of behaviour in an implementation. OBL-F-0021's four dated steps rest on a web page and on a commit sha (9e17aac6b) that does not exist. The scheme has no grade for "read from a version-control record or a published document", which is what the entire atlas is. | unsupported as written | Add a documentary grade and re-sort the rows. |
| inventory/MARKET_AUDIT.md 105; OBL-F-0007 | "a commerce subsystem that is off-chain by design", graded JAN09-SOURCE | Source shows MSG_PRODUCT/MSG_TABLE and CReviewDB do not touch the transaction set. That it is "by design" rather than unfinished is a motive attribution the source cannot carry, and CONSTITUTION-REGISTER.md:33 forbids it: "They do not say why." | wrong | "does not touch the transaction set" — the observation without the modal. |
| docs/PQ-SETTLEMENT-CAPACITY.md title, 24–26, 65–71; OBL-F-0026 | "What a 2009-shaped base layer can settle … a hundred million outputs need seven to thirty-three years" | The entire table is computed at BLOCK_BYTES = 1_000_000 — a rule the same repository (OBL-C-0001, OBL-F-0016) documents as a 2010 retrofit that the January design does not contain. Under v0.1's own 32 MiB ceiling the figures divide by 33.55: 79 days under ML-DSA-44, 357 days under SLH-DSA-128f. The finding reverses. The parenthetical concedes the multiplier and then asserts it "changes nothing about the shape" — it changes the conclusion. | wrong | Lead with the 32 MiB table; give 1 MB as the descendant comparison. |
| verify/pq_settlement_capacity.py 47–50 vs verify/pq_signature_cost.py 233–247 | "Every byte count below comes from PQ-SIGNATURE-COST.md §1 and §2" | The migration size is computed as 200 − 65 + new_pk, a byte substitution that drops the scriptPubKey varint's growth from 1 to 3 bytes once the script passes 252 B. Against the sibling script's own model every ML-DSA migration row is 2 B short (1,447/2,087/2,727 vs 1,449/2,089/2,729). Two scripts in one repository model the same output differently. | wrong | Call model_p2pk_spend. |
| verify/pq_signature_cost.py 237–239 | "scriptSig for P2PK = PUSH(sig‖hashtype) → 1 + sig_len + 1", described as "the real serialization" | A single-byte push opcode is only legal to 75 bytes. Every PQ signature and every lattice key needs OP_PUSHDATA2 (3 B) — SLH-DSA-128f's 17,088 B would need OP_PUSHDATA4. Every PQ row in §2 is 2–4 B short, and the model is not v0.1's CScript::operator<<. | wrong | Model the push opcode by length. |
| docs/PQ-SIGNATURE-COST.md 84 vs docs/PQ-SETTLEMENT-CAPACITY.md 35 | "tx/1MB block 5,000" vs "per block 4,999" | Same scheme, same block size, two answers, in documents that cite each other. The capacity script subtracts a 135 B coinbase; the cost script does not. | wrong | Pick one and state the overhead. |
| docs/PQ-SIGNATURE-COST.md 39–47, 106–108 | "verify 12.4 ms" for ML-DSA against a "measured process-spawn floor of 13.34 ms", then "subtract the floor" | Three of seven verify measurements are below the floor they are to be subtracted from, which gives a negative cryptographic cost; ML-DSA-44 also "signs" faster than ECDSA (18.0 vs 18.7 ms). The measurements are spawn-dominated and not commensurable with the floor, and §3's headline ("verification is essentially free") is drawn from them. | unsupported as written | Measure in-process, or report only that the signal is below the noise. |
| docs/PQ-SIGNATURE-COST.md 18–19 vs 63 | "200 samples per scheme across 8 keys" vs "20 keys × 60 signatures = 1,200 samples" | Two sampling regimes in one document; the §1 table's "distinct 4" is never tied to either. | style | Say which produced which table. |
| docs/PQ-SIGNATURE-COST.md 73–74, 136–137 | "'71 bytes' is widely stated as a constant"; "Bitcoin (2026), this laboratory's own experimental chain, uses (51 of 51 outputs)" | No source for "widely stated". The 51 outputs are 51 coinbases the laboratory itself mined on its own 51-block chain, offered as evidence of output-type prevalence — the laboratory chose the output type. | self-asserted | Cite, or drop; and drop the 51-of-51 as evidence of anything but its own configuration. |
| docs/WHITEPAPER.md 29–31 | the bytes come "from the chain itself, with nobody in between" | The command printed three lines above reads https://blockstream.info/api/tx/… (verify/whitepaper_from_chain.py:29). WHITEPAPER_PROVENANCE.md:101 says so plainly: "from a public API so no node is required". The published digest catches tampering; "nobody in between" is still false. | wrong | "from a public API, checked against the published digest". |
| docs/WHITEPAPER_PROVENANCE.md 104 | "Proof-of-work cannot be backdated." | An absolute, and the laboratory's own derivatives/retarget/ module forges block timestamps to demonstrate the timewarp property. Proof-of-work fixes ordering and cost, not the stamped date. | wrong | "cannot be produced after the fact for a block already extended". |
| docs/NOV08_GENESIS.md 92–93 | "cannot be compiled from what survives … circulated as a preview of the design, privately, to a handful of correspondents" — OBL-F-0005 grades the document NOV08-SOURCE | "cannot" is an absolute where "does not, from the files listed above" is what the inventory shows. "A handful" is a quantity in no quoted record; the note's own quotations support "a pre-release sourcecode preview" and "available by request", not a count. Source cannot establish circulation history. | unsupported as written | Bound the first clause to the inventory; drop the count. |
| RIGHTS.md 90 vs docs/EVIDENCE_POLICY.md 39 | "Several published claims here have been wrong and were corrected in place" vs "A sealed set is not edited in place." | The two statements of the correction discipline contradict each other, in the two documents that define it. | wrong | Align them. |

Where the language rule holds, and I looked. No jurisdiction is named anywhere in the public text — the only hits for SEC are SEC1 key encodings. Motive language is almost entirely the laboratory describing its own choices ("deliberately not published", "by design"), which the rule does not reach; OBL-F-0007 is the one clear breach. The absolutes I found are few and mostly in framing rather than in claims, and one of them — "Corrections … never made silently" in SCRIPT-LIMITS-RETROFITTED.md — was found and removed by the laboratory in revision 3 on 14 September, with r1 and r2 kept beside it. That is the discipline working, and it should be said. The r4 note's "Cause, on the author's account: the host mini-PC powered down" is exactly the right register for an unverifiable fact. And on item 4: every figure in PQ-SIGNATURE-COST.md §2 and PQ-SETTLEMENT-CAPACITY.md §§1–3 reproduces exactly from the stated inputs — sizes, multipliers, per-block counts, per-day counts, the population tables, the 19×–86× range, the "seven to thirty-three years" — and all seven signature and key sizes match FIPS 204 and FIPS 205. The arithmetic is not where this record fails.

The single weakest point, revised. Last pass I said it was the unobtainable January 2009 source. It is worse than that, and the binding records show why. The laboratory built one genuinely original instrument — a pre/post capture that binds an artifact to a running process image, not to a file on disk — and it published twenty-four of those records, every one of which binds c3f15fc5…, its own v0.1.3 build. The run it grades JAN09-EXECUTED, the one claim in the whole register that asserts the historical binary itself mined, relayed, reorganised and spent, has no binding record in the repository, no digest for C:\obl\bitcoin.exe, and a manifest that does not hash the client — while its own prose boasts that "the artifacts are bound to the executing image, not just to a file in an archive". The same substitution runs through OBL-F-0003, where "the January 2009 interpreter" turns out on the next page to be a laboratory compilation against laboratory shims, still graded JAN09-EXECUTED. So the grade that separates this project from a careful reimplementation — the one that says the 2009 object did this, not our model of it — is the grade with the thinnest paperwork behind it, in a repository that timestamps its README. Everything downstream inherits that: the constitution register's witness cells, the "two rules nobody wrote", the claim that the January design is a distinct constitutional object rather than a reconstruction of one. The laboratory's own standard is that a claim about a 2010 commit should name the hash it read. Its executed claims about a 2009 binary do not name the hash they ran.
